import { css } from "@emotion/react";

const horizon = css`
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;

`

export const utilSet = {
    horizon
}